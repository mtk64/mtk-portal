# =====================================================================
# File    : .github/workflows/qa-portal-anchor.yml
# Name    : QA - Portal Anchor Check
# Purpose : Minimal stabile QA-Version (sichtbar + startbar)
# Version : v1.4-safe (2025-08-18)
# Notes   : Erst sicher laden/starten, dann Features wieder anheben
# =====================================================================

name: QA - Portal Anchor Check

on:
  # Manuell im UI startbar
  workflow_dispatch:
    inputs:
      url:
        description: "Portal-URL (leer = automatisch)"
        required: false
        default: ""
  # Automatisch nach Deploy
  workflow_run:
    workflows: ["Manual Commit + Deploy"]
    types: [completed]

permissions:
  contents: read

jobs:
  qa:
    # bei workflow_run nur laufen, wenn Deploy success
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest

    steps:
      - name: Compute site URL
        id: site
        run: |
          if [ -n "${{ github.event.inputs.url }}" ]; then
            echo "url=${{ github.event.inputs.url }}" >> $GITHUB_OUTPUT
          else
            echo "url=https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/" >> $GITHUB_OUTPUT
          fi
          echo "URL=${{ steps.site.outputs.url }}"

      - name: Fetch portal (no-cache) & text view
        run: |
          curl -sS -H 'Cache-Control: no-cache, no-store, max-age=0' -H 'Pragma: no-cache' \
               "${{ steps.site.outputs.url }}?nocache=$(date +%s)" -o portal.html
          test -s portal.html || { echo "Leere Seite"; exit 1; }
          sed -E 's/<[^>]+>//g' portal.html > portal.txt
          echo "--- Lines with 'Projektanker' ---"
          grep -n "Projektanker" portal.txt || true

      - name: Validate anchor format (regex only)
        run: |
          python3 - << 'PY'
import re, sys, pathlib
t = pathlib.Path("portal.txt").read_text(encoding="utf-8", errors="ignore")
lines = [ln.strip() for ln in t.splitlines() if "Projektanker" in ln]
if len(lines) != 1:
  print(f"FAIL: Erwartet 1 Zeile 'Projektanker', gefunden {len(lines)}")
  for l in lines[:5]: print(">>", l)
  sys.exit(2)
line = lines[0]
rx = re.compile(
  r"Projektanker:\s*PRJ-\d{4}-\d{2}-[A-Z0-9-]+"
  r"\s*·\s*Build\s+\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}Z"
  r"\s*/\s*\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}\s+Europe/Berlin"
  r"\s*·\s*[a-z0-9/_-]+"
  r"\s*·\s*#[0-9a-f]{7}"
  r"\s*·\s*Run\s+\d+\s*$",
  re.IGNORECASE
)
if not rx.search(line):
  print("FAIL: Regex-Match fehlgeschlagen.")
  print("LINE:", line)
  sys.exit(3)
print("PASS")
PY

      - name: Summary
        if: always()
        run: |
          echo "## QA - Portal Anchor (safe)" >> "$GITHUB_STEP_SUMMARY"
          echo "- URL: ${{ steps.site.outputs.url }}" >> "$GITHUB_STEP_SUMMARY"
